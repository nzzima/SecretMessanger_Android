# Локальные пути SDK — у каждой машины свои
local.properties

# Сборка и кэши
.gradle/
build/
app/build/
.kotlin/
*.apk
*.aab

# google-services.json под версией намеренно: без него не собирается ни одна машина,
# а репозиторий приватный. Секретов файл не содержит — только идентификаторы проекта.

# Android Studio / IntelliJ: проект открывается из Gradle-файлов
.idea/
*.iml
captures/

.DS_Store
